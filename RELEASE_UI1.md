# BDSpoofer 1.8.1 UI1.2 / 卍解 1.0.2 UI1

基于插件 1.8.1 和卍解 1.0.2，合入审核通过的界面与独立定向参数功能。

## UI 同步与容器状态

插件首页改为紧凑状态卡，分别显示基础功能、高级功能、定向指纹和反关联增强。高级区只列出已开启的百度身份参数、系统硬件参数、防越狱检测，不显示四个默认关闭项；基础、高级、定向各自显示是否执行过一键随机。

卍解为每个容器分别记录基础、高级、定向随机状态。容器标题只标记“当前”，不再重复显示 Crane 的“默认”标记；名称本身为“默认”的容器保留原名。三个彩色按钮使用容器实际宽度重新布局，左右边距一致并完整显示四角圆角。

未选择且从未随机过定向指纹时，配置文件不再保存 17 个备用定向值，也不保存容器绝对路径。基础模式的模拟 XML 配置为 3332 字节，保持在一个 4KB 文件系统块内；首次执行定向随机后才保留定向参数。删除某个 Crane 容器时，其随机状态随该容器配置一同删除，不影响其他容器。

## UI1.2 金额统计上报控制

反关联参数组新增「阻止金额统计上报」开关，默认关闭。关闭时保持原上报行为；开启后只阻止已确认的收益页金额浏览埋点：HTTPS `h2tcbox.baidu.com/ztbox`、`action=zpblog`、`data.actiondata.id=10290`、`page=y_mission_index`、`type=c_pv`，并且必须含有 `ext.num`。该功能不伪造金额，也不修改服务端余额。原生请求由 `NSURLProtocol` 返回本地空响应；网页图片埋点在创建请求前停止，因此没有转发会话、响应转发或重定向生命周期。

## UI1.1 显示修正

插件首页分别显示基础参数与定向参数。定向区展示总开关、五项子开关与各自保存值；从设置页返回时刷新。三组随机、配置文件与 Hook 逻辑保持不变。卍解继续使用上一版 1.0.2 UI1。

## 配套文件

- `BDSpoofer_1.8.1_UI1.2.dylib`：通过巨魔注入器替换百度内旧插件，避免同时保留两个版本。
- `BDSpooferCraneManager_1.0.2-ui1_RootHide.deb`：卍解配套更新，包版本 `1.0.2+ui1`，应用构建号 103。
- 百度 Bundle ID：`com.baidu.BaiduMobileInfo`。两个程序支持 arm64 / arm64e，最低 iOS 15。

## 界面和默认设置

三颗等宽按钮纵向排列，基础为红色、高级为绿色、定向为蓝色。
基础功能设置、高级功能设置、反关联增强合并成一个“反关联设置”入口；二级页面按“基础参数 / 高级参数 / 反关联参数”展开。
常规 21 个开关在首次创建配置时开启；已保存的手动开关状态在升级及重开时保留。
Keychain、App Group、WebKit Cookie、自定义 User-Agent、阻止金额统计上报五项默认关闭。
全局 UIKit 屏幕改写保持关闭，五个定向选择默认关闭。

卍解在读取容器列表时初始化缺失配置，不用先点击随机。新容器的五个身份值只补齐缺失项，已有值保留。
卍解随机写入容器配置后，插件在百度重新进入前台及打开面板时重新加载配置，面板显示与卍解写入的机型信息保持一致。
“恢复安全”关闭所有开关，保留参数；重开不会重新开启这些已保存的关闭状态。

## 三组随机独立

- 基础：只更新基础设备、系统、运营商等参数；保留高级身份、定向参数及所有开关。
- 高级：只更新 IDFA、IDFV、DeviceID、CUID、UTDID 五项身份参数。
- 定向：只更新所选类别的定向参数；不更新基础参数或高级身份。没有选择项目时不执行。

公共系统接口使用基础参数；选中的百度内部出口使用定向参数；未选中的定向出口调用原实现。
两组参数独立，因此分别随机后可以不同。IDFV 始终使用高级身份仓库，不另生成一份定向 IDFV。

## 定向修复

- 识别 UA 缓存中的 `platform_systemVersion` 短串，保留平台前缀及应用版本。
- Push 只替换已核实的 `device_name` 字段，正确编码值，保留其余字段。
- 屏幕独立保存所选机型元数据，避免只选屏幕时沿用旧机型的状态栏与安全区判断。

## 验证边界

配置逻辑测试覆盖默认开关、保留手动设置、三组随机互不覆盖、全部 32 种定向选择以及恢复安全；插件额外覆盖 UA、Push、独立屏幕回归案例。
交付包使用 Xcode 编译并检查签名、架构及包内容。需要替换安装后检查界面和实际使用效果。

这次不改反越狱检测底层实现，不代表已经通过百度越狱检测。相关排查暂停，待这套界面版本交付后再处理。
