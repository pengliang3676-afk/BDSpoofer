#!/bin/sh
set -e
# Backups are user data and are deliberately retained when uninstalling.
if command -v uicache >/dev/null 2>&1; then
    uicache -u com.peng.hmcleaner >/dev/null 2>&1 || true
fi
exit 0
