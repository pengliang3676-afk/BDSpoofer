name: Build BDSpoofer

on:
  push:
    branches: [main, master]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-14

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Show Xcode and SDK info
        run: |
          xcodebuild -version
          xcrun --sdk iphoneos --show-sdk-path
          xcrun --sdk iphoneos --show-sdk-version

      - name: Build arm64
        run: |
          SDK_PATH=$(xcrun --sdk iphoneos --show-sdk-path)
          xcrun --sdk iphoneos clang \
            -arch arm64 \
            -isysroot "$SDK_PATH" \
            -miphoneos-version-min=15.0 \
            -fobjc-arc \
            -dynamiclib \
            -framework Foundation \
            -framework UIKit \
            -framework CoreGraphics \
            -framework AdSupport \
            -framework CoreTelephony \
            -framework Security \
            -framework WebKit \
            -install_name @rpath/BDSpoofer_1.6.0.dylib \
            -o BDSpoofer_1.6.0_arm64.dylib \
            BDSpoofer.m

      - name: Build arm64e
        run: |
          SDK_PATH=$(xcrun --sdk iphoneos --show-sdk-path)
          xcrun --sdk iphoneos clang \
            -arch arm64e \
            -isysroot "$SDK_PATH" \
            -miphoneos-version-min=15.0 \
            -fobjc-arc \
            -dynamiclib \
            -framework Foundation \
            -framework UIKit \
            -framework CoreGraphics \
            -framework AdSupport \
            -framework CoreTelephony \
            -framework Security \
            -framework WebKit \
            -install_name @rpath/BDSpoofer_1.6.0.dylib \
            -o BDSpoofer_1.6.0_arm64e.dylib \
            BDSpoofer.m

      - name: Create fat binary and verify
        run: |
          lipo -create BDSpoofer_1.6.0_arm64.dylib BDSpoofer_1.6.0_arm64e.dylib -output BDSpoofer_1.6.0.dylib
          echo "=== file type ==="
          file BDSpoofer_1.6.0.dylib
          echo "=== dependencies ==="
          otool -L BDSpoofer_1.6.0.dylib
          echo "=== platform ==="
          otool -l BDSpoofer_1.6.0.dylib | grep -A5 LC_BUILD_VERSION
          echo "=== interpose sections ==="
          otool -l BDSpoofer_1.6.0.dylib | grep -A2 "__interpose" || true

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: BDSpoofer-1.6.0
          path: |
            BDSpoofer_1.6.0.dylib
            bdspoofer_config.plist
